
import 'package:flutter/material.dart';

class HeaderBookTicket extends StatelessWidget {
  const HeaderBookTicket({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(

    );
  }
}
